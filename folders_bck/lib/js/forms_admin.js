/* 
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */







/*

function  createditor(myclass, type) {
    $().ready(function() {


        $(myclass + "_show").toggle(function() {

            $(myclass).tinymce().hide();
        }, function() {

            $(myclass).tinymce().show();
        })

        $(myclass + "_reload").click(function() {



            $(myclass).html($(myclass + "_def").val());
        })






        if (type == "textarea") {
            $.getScript("../includes/js/jscripts/tiny_mce/tinymce.min.js").done(function() {
                tinymce.init({
                    selector: myclass,
                    plugins: [
                        "the plugins I need"
                    ],
                    toolbar: "the tools I need"
                });
            });
          

        } else {
             $.getScript("../includes/js/jscripts/tiny_mce/tinymce.min.js").done(function() {
                tinymce.init({
                    selector: myclass,
                    plugins: [
                        "the plugins I need"
                    ],
                    toolbar: "the tools I need"
                });
            });
        }


    })

}

*/



